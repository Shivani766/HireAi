{
  "watch": ["*.js", "routes", "controllers", "models", "middlewares", "Services", "utils"],
  "ignore": ["node_modules", ".env"],
  "delay": 800,
  "ext": "js",
  "signal": "SIGTERM",
  "killSignal": "SIGKILL",
  "timeout": 3000,
  "env": {
    "NODE_ENV": "development"
  }
}
